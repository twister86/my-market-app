# Production-???????.
# ??? ??????? ??????? ?? ?????????? ????????? ??? Vault ? ?? ??????? ? ?????.
#
# ?????? 1 ? ?????????? ????????? (Docker / Kubernetes):
#   AUTH_CLIENT_SECRET=<strong-random-secret>
#   AUTH_RSA_PUBLIC_KEY=<base64-encoded X.509 public key>
#   AUTH_RSA_PRIVATE_KEY=<base64-encoded PKCS8 private key>
#
# ?????? 2 ? Spring Cloud Vault:
#   spring.cloud.vault.uri=https://vault.example.com
#   spring.cloud.vault.token=${VAULT_TOKEN}
#   spring.config.import=vault://secret/auth-server
#
# ??? ????????????? ?????:
#   openssl genrsa -out private.pem 2048
#   openssl pkcs8 -topk8 -nocrypt -in private.pem -out private_pkcs8.pem
#   openssl rsa -in private.pem -pubout -out public.pem
#   # ?????? ????????? ? ???????? ?????:
#   grep -v "\-\-\-" public.pem | tr -d '\n'       -> ???????? AUTH_RSA_PUBLIC_KEY
#   grep -v "\-\-\-" private_pkcs8.pem | tr -d '\n' -> ???????? AUTH_RSA_PRIVATE_KEY

auth.client.secret=${AUTH_CLIENT_SECRET}
auth.rsa.public-key=${AUTH_RSA_PUBLIC_KEY}
auth.rsa.private-key=${AUTH_RSA_PRIVATE_KEY}
